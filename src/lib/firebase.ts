// This file is kept for compatibility but we're now using the backend service
// The backend service provides the same functionality without needing Firebase credentials

export const auth = null;
export const db = null;
export const app = null;
